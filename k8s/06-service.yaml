apiVersion: v1
kind: Service
metadata:
  name: ritual-ledger
  namespace: ritual-ledger
  labels:
    app.kubernetes.io/name: ritual-ledger
    app.kubernetes.io/component: app
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: ritual-ledger
    app.kubernetes.io/component: app
  ports:
    - name: http
      port: 80
      targetPort: http
